<aside class="risk-flag-widget" aria-label="active risk flags">
  <h3 class="govuk-heading-s">Active alerts</h3>
  <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">
  {% if params.flags != null %}
  {% if params.flags.length %}
  <ul class="govuk-list" aria-label="Risk flags">
    {% for risk in params.flags %}
    <li>{{ risk.description }}</li>
    {% endfor %}
  </ul>
  {% else %}
  <p>No risks</p>
  {% endif %}
  {% else %}
  <p>Something went wrong. We are unable to show risk flags at this time. Try again later.</p>
  {% endif %}
  <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">
  {% if params.lastUpdated %}
    {{ params.lastUpdated | safe }}
  {% endif %}
</aside>
